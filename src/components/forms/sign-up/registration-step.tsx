import { useAuthContextHook } from "@/context/use-auth-context";
import { useFormContext } from "react-hook-form";

type Prop = { };

const RegistrationFormStep = (props:Prop)=>{
    const { register, formState:{errors},setValue  } = useFormContext();
    

    return(

    )
}